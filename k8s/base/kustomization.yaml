apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - externalsecret-oauth2.yaml
  - oauth2-proxy.yaml
  # No Ingress: public .co traffic enters via the cloudflared tunnel -> the
  # oauth2-proxy Service (Google gate). See the infra repo's tunnels.tf.
